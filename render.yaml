services:
  - type: web
    name: flask-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 4 --bind 0.0.0.0:$PORT --timeout 60
    plan: free
    autoDeploy: true
